{
  "name": "swipe-vote-app",
  "version": "1.0.0",
  "description": "Mobile-first swipe-to-vote web app with a Node backend and SQLite persistence.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --no-warnings server/server.js",
    "dev": "node --no-warnings --watch server/server.js",
    "seed": "node --no-warnings scripts/seed.js",
    "add-item": "node --no-warnings scripts/add-item.js",
    "check": "node --no-warnings scripts/smoke-test.js"
  },
  "dependencies": {
    "ws": "^8.20.1"
  }
}
